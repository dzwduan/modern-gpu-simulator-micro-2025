# Repo-Hygiene Verification Record

## Status

Stage 阶段一（仓库卫生）of `docs/plans/2026-07-17-structure-refactor-roadmap.md` is
complete except one deletion, which is blocked by a genuine in-repo reference
and reported instead of forced through. No `.cc`/`.h`/`Makefile` file and
nothing under `simulator-remodeled/gpu-simulator/{gpgpu-sim/src,trace-driven,
trace-parser}` or `tests/remodeled_trace/` was touched. All changes are
documentation, gitignore, and dead-scaffolding deletions; the regression gate
is re-run below to prove zero behavioral drift.

## Work items: done / skipped

| # | Item | Result |
| --- | --- | --- |
| 1 | Untracked plain-rm scaffolding | Done: `comparison_results/`, `tests/build/`, `refactoring-audit.html` (+ reference rewrite) |
| 1 | Tracked dead scaffolding | Done: `docs/refer.md`, `docs/plans/claude-plan.md`, `simulator-remodeled/util/plotting/`, `simulator-remodeled/.travis.yml` |
| 1 | `simulator-remodeled/util/hw_stats/` | **Skipped / blocked** — see below |
| 1 | Upstream leftover docs | Done: `simulator-remodeled/{README.md,release.notes.md,AccelWattch.md}`, `simulator-remodeled/gpu-simulator/gpgpu-sim4.md` |
| 2 | `AGENTS.md` → pointer | Done |
| 3 | `.gitignore` targeted rewrite | Done, `git status --short` identical before/after |
| 4 | README consolidation (`usage.md` + `run.sh`) | Done, both files removed |
| 4 | `arch.md` entry page + capability-matrix fix | Done |
| 4 | `01-总览与假设集合.md` reconciliation | Done, minimal insertion only |
| 4 | `simulator-remodeled/docs/plans/` → `docs/plans/` | Done, empty tree removed |
| 4 | `09-源码锚点索引.md` stale call-chain fix | Done |
| 4 | `docs/detailed-design/README.md` index | Done, created |
| 4 | Relativize paths in `2026-02-25-arch-remodeling-truepath-implementation.md` | Done, 14 occurrences |
| 5 | Archive `refactor/true-path-scoreboard-cleanup` | Done: annotated tag + local branch delete; remote untouched |
| 6 | Roadmap status row | Done in final commit |

An incidental item outside the enumerated list: an untracked root-level
`gpgpu_inst_stats.txt` (byte-identical stray copy of the tracked
`simulator-remodeled/gpgpu_inst_stats.txt` generated by an earlier manual run
from the repository root) was also removed by plain `rm`. It produced no
tracked diff. This was done to satisfy the gate's literal
"`git status --short` clean except `.codegraph/`" requirement; flagged here
for visibility since it was not in the original work-item list.

## Per-item evidence

### 1. Deletions

Reference-grep method: `grep -rln "<pattern>" --exclude-dir=.git .` run before
each deletion; a hit only inside `docs/plans/2026-07-17-structure-refactor-roadmap.md`
(the roadmap describing this deletion) counts as zero real references.

| Path | Reference grep result | Verdict |
| --- | --- | --- |
| `comparison_results/` | roadmap only | zero refs, deleted (untracked, plain `rm`) |
| `tests/build/` | roadmap only | zero refs, deleted (untracked, plain `rm`; 0 regular files, orphaned CMake configure output) |
| `refactoring-audit.html` | roadmap; `docs/plans/2026-07-15-remodeled-trace-p0-p1-semantic-repair.md:6` | rewired the one real reference to point at the roadmap doc, same commit `b6a8473`; then deleted (untracked, plain `rm`) |
| `docs/refer.md` (0 bytes) | roadmap only | zero refs, `git rm`, commit `7ea9c7d` |
| `docs/plans/claude-plan.md` | roadmap only | zero refs, `git rm`, commit `7ea9c7d` |
| `simulator-remodeled/util/plotting/` | roadmap; `simulator-remodeled/README.md` (deleted same batch); `simulator-remodeled/util/plotting/README.md` (self, deleted with it); `gpu-simulator/gpgpu-sim/{Jenkinsfile,nightly.jenkinsfile}` | the two Jenkins files reference `./gpgpu-sim_simulations/util/plotting/...` — a path inside an external `gpgpu-sim_simulations` checkout that does not exist anywhere in this repository; substring match only, not a real reference to the deleted tree. `git rm`, commit `19dd425` |
| `simulator-remodeled/.travis.yml` | roadmap only | zero refs, `git rm`, commit `19dd425` |
| `simulator-remodeled/util/hw_stats/` | roadmap; `simulator-remodeled/README.md` (deleted same batch); **`simulator-remodeled/util/accelwattch/accelwattch_hw_profiler/profile_validation_perf.sh:48`** (`$ACCELSIM_ROOT/../util/hw_stats/run_hw.py ...`) | **real, live reference from AccelWattch tooling.** `util/accelwattch/` is explicitly stage-two scope (whole-subsystem deletion per roadmap §4 阶段二), not stage-one scope; fixing or deleting that referrer here would reach into stage two's declared territory. Per the brief's own escape hatch ("fix the referrer in the same commit or report instead of deleting"), this deletion is **skipped and reported** rather than forced. `simulator-remodeled/util/hw_stats/` remains tracked, unmodified. |
| `simulator-remodeled/README.md` | roadmap only (own path never linked elsewhere) | zero refs, `git rm`, commit `44021e0` |
| `simulator-remodeled/release.notes.md` | roadmap only | zero refs, `git rm`, commit `44021e0` |
| `simulator-remodeled/gpu-simulator/gpgpu-sim4.md` | roadmap; `simulator-remodeled/gpu-simulator/README.md:17` | the README line links `https://github.com/accel-sim/accel-sim-framework/blob/dev/gpu-simulator/gpgpu-sim4.md` — an external URL into the upstream GitHub repo, not a relative link into this file. Not a real reference. `git rm`, commit `44021e0` |
| `simulator-remodeled/AccelWattch.md` | roadmap; `simulator-remodeled/gpu-simulator/README.md:21`; `simulator-remodeled/gpu-simulator/gpgpu-sim/README.md:66` | both are external URLs into `github.com/VijayKandiah/accel-sim-framework`, not relative links into this file. Not a real reference. `git rm`, commit `44021e0` |
| `usage.md`, `run.sh` | roadmap only; the "run.sh" substring also matches `simulator-remodeled/util/job_launching/run_simulations.py`, `simulator-remodeled/util/tracer_nvbit/{run_hw_trace.py,spot_hw_trace.py}`, `simulator-remodeled/util/hw_stats/run_hw.py`, `nv_trace/run_trace.py`, `simulator-remodeled/util/job_launching/README.md` | all six are per-job-run scripts these tools generate at runtime inside temporary run directories (e.g. `os.path.join(this_run_dir, "run.sh")`), unrelated to the deleted root file. Their content was folded into `README.md` first; both `git rm`'d, commit `9a32ade` |
| `simulator-remodeled/docs/plans/*.md` (2 files) | roadmap only | zero refs, `git mv` to `docs/plans/`, empty tree removed, commit `e7b82ce` |
| `refactor/true-path-scoreboard-cleanup` (branch) | n/a | annotated tag `archive/true-path-scoreboard-cleanup` created pointing at `1969355239f594f127f828e58b9a74ec128f4d24` (verified with `git rev-parse` before and after tagging); local branch deleted with `git branch -D`; `remotes/origin/refactor/true-path-scoreboard-cleanup` untouched (confirmed present in `git branch -a` after) |

Final `git ls-files <path>` sweep (all 11 deleted/moved tracked paths return
empty; commands and output are in this session's tool transcript):
`docs/refer.md`, `docs/plans/claude-plan.md`,
`simulator-remodeled/util/plotting`, `simulator-remodeled/.travis.yml`,
`simulator-remodeled/README.md`, `simulator-remodeled/release.notes.md`,
`simulator-remodeled/gpu-simulator/gpgpu-sim4.md`,
`simulator-remodeled/AccelWattch.md`, `usage.md`, `run.sh`,
`simulator-remodeled/docs` — all empty.

### 3. `.gitignore` rewrite — before/after `git status --short`

Captured immediately before and after editing `.gitignore` (with all prior
deletions from this stage already applied and committed):

```
before: ?? .codegraph/
after:  ?? .codegraph/   ( M .gitignore, expected — the file itself changed)
```

Identical modulo the `.gitignore` self-diff line. The four JSON paths that
relied on the removed blanket `*.json` rule were enumerated with
`git status --ignored=matching --short | grep -i json` before editing
(`.claude/settings.local.json`, `.vscode/settings.json`,
`compile_commands.json`, `simulator-remodeled/compile_commands.json`) and
individually re-verified `IGNORED` after via `git check-ignore -q` on each
path. `nv_trace/hw_run/` and `simulator-remodeled/hw_run/` JSON fixtures
under those trees were already covered by their own nested `.gitignore`
`hw_run/` rules (`nv_trace/.gitignore:14`, `simulator-remodeled/.gitignore:2`),
confirmed with `git check-ignore -v`, so they never depended on the blanket
rule and needed no new entry. All five tracked whitelisted JSON files
(`tests/remodeled_trace/{cases,goldens}.json` and the three fixture
`*.metadata.json`) remain tracked and are unaffected by gitignore rules
(`git ls-files "*.json"` unchanged). The two `./simulator-remodeled/
gpu-simulator/{build,bin}` lines were dropped after confirming
`simulator-remodeled/gpu-simulator/.gitignore` still contains `bin/` and
`build/`.

### 4. Documentation fixes

- `docs/detailed-design/09-源码锚点索引.md`: `Subcore::fetch_L0I_cycle()` did
  not exist anywhere in source (`grep -rn "fetch_L0I_cycle"` returned zero
  hits before the edit, confirming it was already dead/wrong); the correct
  function is `Subcore::fetch(SM*)` at
  `simulator-remodeled/gpu-simulator/gpgpu-sim/src/gpgpu-sim/remodeling/subcore.cc:963`.
  The `SM::cycle()` phase-order diagram was corrected by reading
  `SM::cycle()` at `.../remodeling/sm.cc:235-274` directly: the real order is
  `L0_icnt::cycle()` (via `m_icnt_L0s`), then `m_L1I_L1_half_C_cache->cycle()`,
  then `m_shared_dp_unit->cycle()` (conditional), then
  `m_ldst_unit_shared_of_sm->cycle()`, then each `Subcore::cycle()`
  (which itself ends with the subcore's private `m_L0C_cache->cycle()` and
  `m_L0I->cycle()`, per `subcore.cc:100-117`), then the two
  `consume_pending_wait_barrier_actions` passes, then the dispatch-wait
  counter decrement. No source file was modified; only the doc diagram.
- `arch.md` / `docs/detailed-design/01-总览与假设集合.md`: both previously
  stated `is_interwarp_coalescing_enabled`, `is_instruction_prefetching_enabled`,
  and `is_fp32_and_int_unified_pipeline` as fixed `true` invariants. Per
  `docs/plans/2026-07-15-remodeled-trace-p0-p1-semantic-repair.md` §§2-3
  (source of truth), these three are a configurable support matrix, not
  invariants. `arch.md` was rewritten as a short entry page stating the
  actual fixed invariants versus the configurable matrix, citing the plan.
  `01-总览与假设集合.md` received a minimal insertion (one clarifying
  blockquote before its assumption tables, one amended summary sentence)
  rather than a rewrite, preserving its paper-alignment validation content.
- `docs/detailed-design/README.md`: new index, all 19 relative links verified
  to resolve with a scripted existence check (see "Markdown links" below).

### 5. Dependency-direction baseline — unchanged (no code touched)

Re-running the safety-net record's grep evidence at the end of this stage:

```
$ grep -rln '#include.*remodeling/' --include="*.cc" --include="*.h" . \
    | grep -v '^\./gpgpu-sim/remodeling/'
abstract_hardware_model.cc
gpgpu-sim/gpu-sim.cc
gpgpu-sim/gpu-sim.h
gpgpu-sim/scoreboard.cc
gpgpu-sim/scoreboard_reads.cc
gpgpu-sim/shader.cc
gpgpu-sim/shader_core_wrapper.h
gpgpu-sim/shader.h
```

(run from `simulator-remodeled/gpu-simulator/gpgpu-sim/src/`)

```
$ grep -n "functional_unit" abstract_hardware_model.h
66:class functional_unit;
1653:  void set_fu_assigned(functional_unit *fu) { m_fu_assigned = fu; }
1654:  functional_unit *get_fu_assigned() const { return m_fu_assigned; }
1713:  functional_unit *m_fu_assigned; // MOD. Remodeling
```

Both outputs are identical in file set and line count to the
`2026-07-17-safety-net.md` baseline table (8 reverse-include files; 4
`functional_unit` sites in `abstract_hardware_model.h`). This is direct
evidence that no source file was touched during this stage — the roadmap's
per-stage dependency-direction ledger carries forward unchanged.

## Gate

All commands run from the repository root, after all commits below.

| Command | Exit code | Result |
| --- | ---: | --- |
| `python3 -m unittest discover -s tests -v` | 0 | 21 tests OK |
| `python3 tests/remodeled_trace/run_regression.py check` | 0 | 4/4 `passed`, 0 `failed`, 0 `observed_not_golden`; stats byte-identical to the `2026-07-17-safety-net.md` table (e.g. `gpu_tot_sim_cycle` 192072/4086/13042/28795 across the four cases) |
| `git status --short` | — | `?? .codegraph/` only |
| `git ls-files <deleted-path>` (11 paths) | — | empty for all |

The simulator binary
(`simulator-remodeled/gpu-simulator/bin/release/accel-sim.out`,
sha256 `2bd89f68988e1d550867d9b8a96541d1a83831c289f3848706a81b3464b8e508`,
mtime predating this session) was not rebuilt, per this stage's hard boundary
against touching `.cc`/`.h`/`Makefile` files; it is the artifact left in the
tree by the prior safety-net stage's review-response fix. Its hash differs
from the safety-net record's original pre-review-fix table entry by design
(that record's own "Review response" section documents a later rebuild after
a config-sizing fix); this stage neither rebuilt it nor could have, and the
regression gate re-run above confirms it still produces the exact approved
goldens.

### Markdown links created or rewired

| File | Links checked | Result |
| --- | --- | --- |
| `arch.md` | 1 (`docs/plans/2026-07-15-remodeled-trace-p0-p1-semantic-repair.md`) | resolves |
| `docs/detailed-design/README.md` | 19 (18 chapter links + `../../arch.md`) | all resolve |
| `docs/plans/2026-03-06-rtx4090-config-{design,plan}.md` (relocated) | 0 links present | nothing to break |

Checked with a scripted existence test (`grep -oP '(?<=\]\()[^)]+(?=\))' <file>`
piped through a per-target `[ -e "$target" ]` check, run from each file's own
directory).

## Commits (chronological)

| Hash | Subject |
| --- | --- |
| `b6a8473` | docs: point semantic repair plan at the structure roadmap |
| `7ea9c7d` | chore: delete empty and orphaned planning scratch docs |
| `19dd425` | chore: delete unused upstream plotting and CI leftovers |
| `44021e0` | chore: delete upstream fork-description leftovers |
| `021187e` | docs: point AGENTS.md at CLAUDE.md |
| `92737eb` | chore: replace blanket JSON gitignore with targeted entries |
| `9a32ade` | docs: consolidate build/trace/run recipe into README |
| `7b4e29e` | docs: reduce arch.md to an entry page and fix the True-Path/matrix conflict |
| `c2e8730` | docs: fix stale SM::cycle() call chain and add chapter index |
| `e7b82ce` | docs: relocate simulator-remodeled plan docs under root docs/plans |
| `f1565aa` | docs: relativize absolute paths in arch remodeling implementation plan |

Non-commit ref changes: annotated tag `archive/true-path-scoreboard-cleanup`
created at `1969355239f594f127f828e58b9a74ec128f4d24`; local branch
`refactor/true-path-scoreboard-cleanup` deleted (remote untouched).

The roadmap status-table update and this record are added together in one
further commit on top of `f1565aa`, per this stage's brief.

## Open items / blockers

- `simulator-remodeled/util/hw_stats/` was **not deleted**. It has a live
  reference from `simulator-remodeled/util/accelwattch/
  accelwattch_hw_profiler/profile_validation_perf.sh:48`
  (`$ACCELSIM_ROOT/../util/hw_stats/run_hw.py ...`). That referrer belongs to
  the AccelWattch subsystem, which the roadmap's 阶段二 scope deletes as a
  whole (`src/accelwattch/`, `power_interface`/`power_stat` hooks,
  `util/accelwattch/`). Fixing or deleting the referrer here would mean
  editing AccelWattch-owned tooling one stage early for a fix that becomes
  moot the moment 阶段二 deletes the whole subsystem. Left tracked and
  unmodified; carried forward as a 阶段二 cleanup item alongside the rest of
  `util/accelwattch/`.
- Independent read-only review of this stage's commits is the coordinator's
  responsibility per the roadmap's execution model (§3) and is not part of
  this record.
- `.codegraph/` remains intentionally untracked, as in the safety-net record.

## Review response

The independent read-only review of `ff33f2a..df6c424` reported two findings,
both confirmed applicable and both resolved by documentation fixes (per the
deletion principle, instructions for removed tools are removed, not the other
way around):

1. README run sequence was incoherent: item 1 traces `GPU_Microbenchmark` but
   the follow-up batch example ran `-B rodinia_2.0-ft` with an AccelWattch
   config and labeled itself "from item 1", and the direct-run example pointed
   at a `./hw_run/Ampere/...` path that no documented step produces. Fixed:
   the batch example now runs `-B GPU_Microbenchmark -C RTX4090` (config name
   verified in `util/job_launching/configs/define-standard-cfgs.yml`), and the
   direct-run example extracts `exampleTraces/rodinia2Ampere.tar.gz` and uses
   the real extracted path (verified against the archive listing).
2. `util/job_launching/README.md` still directed users to the deleted
   `../plotting/plot-get-stats.py` and `../plotting/plot-correlation.py`
   (relative references missed by the deletion grep, which searched the
   `util/plotting` literal). Fixed: the two stale instruction blocks are
   removed.

Machine verification of the corrected direct-run example (trace extracted to a
scratch directory, `OMP_NUM_THREADS=1`): exit 0, two kernels launched,
`GPGPU-Sim: *** exit detected ***` — the documented command sequence now runs
end to end. Post-fix gate: `python3 -m unittest discover -s tests` exit 0;
`python3 tests/remodeled_trace/run_regression.py check` exit 0, 4/4 passed.

Lesson recorded for later deletion stages: reference grep must include
relative-path forms (`../<dir>/`) and filename-only forms, not just the
repo-relative literal.
