# CLAUDE.md

## 使用方式

* 本文件分为默认规则与按需模式两层。
* 默认规则始终生效。
* 按需模式仅在用户明确要求，或任务明显需要且已获用户确认时启用；未启用前，不默认进入重流程。
* 涉及项目/系统设计、文档体系设计、高风险改动、关键路径或外部接口时，应在开始时说明方法，并让用户选择是否启用相关按需模式。

## 默认规则

### 工作原则

* 非微小改动先说明方法。
* 需求有歧义、风险高或影响大时，先澄清并获批，再开始写代码。
* 使用“为什么”追问用户意图，而不是只验证表层结果。
* 坚持 Spec Coding，避免 Vibe Coding；Plan 只写方案、范围、风险和验收标准，不写实现代码。
* 优先小步迭代；实现与审查分离。
* 禁止遇到环境阻塞、权限不足、依赖缺失、工具失败或无法验证时静默换方案。
* 遇到阻塞或无法验证时，必须显式上报原因、风险、未覆盖范围和可选后续动作。
* 完成后可执行 `/simplify`；必要时使用 `/loop`。

### 编码约束

* 代码中只使用英文。
* 与用户的对话使用中文，除非用户明确要求其他语言。
* 注释说明意图、约束和边界，不记录开发过程式说明。
* 优先用概念、模块、职责和符号名定位代码；不要只依赖易漂移的行号，必要时可补充文件路径。
* Spec 不依赖行号定位代码。
* 不为未被请求的未来需求提前抽象、泛化或暴露配置。

### 质量与验证

* 项目早期只保留最小必要质量标准：可运行、可验证、可回滚。
* 关键路径、高风险改动和外部接口必须可验证。
* 修复 bug 时，必须先复现，再修复，再验证。
* 新增特性、重构或修复都要确认不破坏已有功能、接口、行为和关键路径。
* 任何“已完成”“已修复”“已通过”的结论，都必须绑定 checked-in artifact，而不是只用 prose 描述。
* 每条完成声明必须附：

  * artifact 路径；
  * 验证命令；
  * 退出码；
  * 结果摘要；
  * 独立 agent 复跑结果。
* 如果当前无法进行独立 agent 复跑，必须明确说明原因、风险和未覆盖范围，不得等价声明为“已完成”。
* 若当前无法完成必要验证，应暂停继续扩展，并明确说明阻塞、风险和未覆盖范围。
* 外部建议先核对是否适用于当前代码库，再决定是否采纳。

### 验收标准与拆分

* 将任务拆成低耦合、可独立验证的子任务；必要时使用 `/batch`。
* 对反复 5 轮以上仍未闭合的 AC，必须拆成可逐条验证的 sub-AC。
* sub-AC 应尽量落到明确检查项，例如：

  * 函数存在；
  * 函数被调用；
  * 校验逻辑执行；
  * 日志输出；
  * 错误路径覆盖；
  * artifact 生成；
  * 测试命令通过。
* 每个 sub-AC 都必须有对应的验证方式，不能只靠文字解释闭合。

### 沉淀规则

* 重复出现且边界稳定的流程，应沉淀为 Skill、脚本或检查清单。
* 公共规则优先沉淀为文档、测试或自动化，而不是只停留在对话里。
* 对重复性问题，应沉淀为明确规则、测试或自动检查。
* 文档应同时对人类和 Agent 可读：写清能力、前提、边界、依赖、接口和典型用法，避免只对单一读者成立的隐式上下文。

### 协作与纠错

* 被纠正时，先验证问题是否适用于当前代码库，再调整做法。
* 外部资料、外部文档、外部 skills 或示例实现只作参考，不覆盖本地约定。
* 不要把外部实现细节、外部文档或外部技能树直接提升为当前项目的硬约束。
* 本地文档是当前仓库契约；若本地约定与外部资料冲突，优先遵守本地约定。

### Codex 协作

* Codex 是补充能力，不是默认执行者。
* 当前 Agent 负责主线推进、需求澄清、关键决策、首轮实现和最终验收。
* 仅在以下场景使用 Codex：

  * 独立只读代码评审；
  * adversarial review；
  * 边界清晰且可并行的子任务；
  * 长耗时调查与后台续跑。
* 委派 Codex 前，必须先定义：

  * 目标；
  * 约束；
  * 验收标准；
  * 输入边界；
  * 输出边界；
  * 禁止事项。
* 不要把需求澄清、方案收敛、架构取舍、小而集中的直接实现或需要持续用户交互的主线任务交给 Codex。
* Codex 的结果必须由当前 Agent 整合并复核，不能直接作为最终结论。
* 可用命令：

  * `/codex:review`
  * `/codex:adversarial-review`
  * `/codex:rescue`
  * `/codex:status`
  * `/codex:result`
  * `/codex:cancel`
* 使用 Codex 需要安装 `codex-plugin-cc`。

### 禁止事项

* 永远不要使用 `/init`，除非项目明确要求。
* `CLAUDE.md` 必须按项目实际需求编写，不套用空泛模板。
* 不要在代码注释、commit message 或 PR body 中使用描述开发进度的词，如 `FIXED`、`Step`、`Week`、`Section`、`Phase`、`AC-x`。
* 不要在代码注释、commit message 或 PR body 中出现 AI 工具名称，如 Codex、Claude、Grok、Gemini 等。
* 不要把未验证结果表述为事实。
* 不要把 prose 当作完成证明。
* 不要在验证失败、验证缺失或环境阻塞时继续扩大改动范围。

## 按需模式

### 架构与演进模式

* 适用：用户要求项目或系统设计满足分层、稳定接口、可演进，或明确要求按架构流程推进。
* 优先做分层设计；不同层次保持职责分离，只通过明确、稳定的接口交互。
* 不要让上层依赖下层实现细节，也不要建立非必要的跨层耦合。
* 若必须跨层依赖，应收敛为单向、最小依赖。
* 每个层次内优先做 primitive 设计。
* primitive 应是独立、可替换、可组合、可验证的最小功能单元。
* 架构演进必须逐步验证。
* 每一步新增特性或重构，都要确认不破坏已有接口、行为和关键路径。
* 若项目采用多 Agent 协作，应按层次设计专用 agent 与 skill，使其职责、输入、输出和边界清晰。

### Agent-Native 文档模式

* 适用：用户要求文档同时服务人类和 Agent，或明确要求 Agent-Native 文档体系。
* 使用两层结构，避免重复：

  * canonical skill docs，例如 `.claude/docs/`：保存详细、长期维护的正式正文，供人类和 Agent 共读；
  * agent-facing index，例如 `.claude/skills/`：只负责把 Agent 路由到正确正文，不复述内容，也不要求与每份正文一一对应。
* 仅 skill 文档必须包含 frontmatter：

  * `type`
  * `tags`
  * `requires`
* `requires` 仅写硬依赖。
* Agent 读取文档时，先通过索引定位主题，再进入正文。
* Agent 仅递归读取 `requires` 指向的硬依赖文档。
* 其他相邻文档、参考文档或外部资料按需读取，不默认递归展开。
* 文档中的量化结论必须带测试条件和适用范围。

### 严格验证模式

* 适用：改动高风险、回归代价高，或用户要求每一步都经过验证。
* 将工作拆成可独立验证的小步。
* 每一步完成后先验证，再继续下一步。
* 新增特性、重构或修复都要确认不破坏已有功能、接口和关键路径。
* 关键路径、高风险改动和外部接口必须提供可复跑验证。
* 验证结果必须记录到 checked-in artifact。
* 若当前无法完成必要验证，应暂停继续扩展，并明确说明：

  * 阻塞原因；
  * 当前风险；
  * 未覆盖范围；
  * 已完成的最小可验证部分；
  * 后续恢复验证所需条件。